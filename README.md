# Car Dealership DB

Dealership management system built with Django, Django REST Framework, and PostgreSQL.

## Requirements

- Docker

## Quick start

```bash
git clone https://github.com/dawidw-km/CarDealershipDB.git
cd CarDealershipDB

cp .env_example .env

docker compose up --build